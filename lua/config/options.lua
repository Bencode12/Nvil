-- Nvil Configuration Options
return {
  -- Stardew Valley Theme Settings
  stardew = {
    enabled = true,
    season = "auto", -- auto, spring, summer, fall, winter
    logo = "village", -- village, farm, town, community
    show_farming_level = true,
    show_seasonal_messages = true,
  },
  
  -- Dashboard Settings
  dashboard = {
    enabled = true,
    show_footer = true,
    show_date_time = true,
  },
}
